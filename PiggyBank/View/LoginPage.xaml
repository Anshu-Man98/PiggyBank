<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="PiggyBank.LoginPage"
    NavigationPage.HasNavigationBar="False"
    BackgroundColor="#176bff">
    <ContentPage.Content>
        <StackLayout Padding="5">
            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                <Label Text="My Piggy" TextColor="Azure" FontSize="30" FontAttributes="Bold" Margin="0,40,0,0"/>
            </StackLayout>
            
            <StackLayout VerticalOptions="CenterAndExpand" Spacing="30">
                <Label Text="Log In" TextColor="Azure" FontSize="30" FontAttributes="Bold" HorizontalTextAlignment="Center"/>
                <StackLayout Orientation="Horizontal" HorizontalOptions="Fill" Spacing="0" Margin="20,0,20,0" >
                    <Label Text="Name: " FontSize="20" TextColor="Azure"/>
                    <Entry x:Name="name" Placeholder="user name" BackgroundColor="#8fb5ff" Text="{Binding UserName}" HorizontalOptions="FillAndExpand" VerticalOptions="End">
                              <Entry.PlaceholderColor>
                    <OnPlatform x:TypeArguments="Color">
                        <On Platform="iOS" Value="white"></On>
                        <On Platform="Android" Value="white"></On>
                    </OnPlatform>
                </Entry.PlaceholderColor>
                        </Entry>
                </StackLayout>

                <StackLayout Orientation="Horizontal" HorizontalOptions="Fill"  Margin="20,0,20,0">
                    <Label Text="Password:" FontSize="20"  TextColor="Azure"/>
                    <Entry x:Name="password" IsPassword="True" Placeholder="enter password" BackgroundColor="#8fb5ff" Text="{Binding Password}" HorizontalOptions="FillAndExpand" VerticalOptions="End" >
                              <Entry.PlaceholderColor>
                    <OnPlatform x:TypeArguments="Color">
                        <On Platform="iOS" Value="white"></On>
                        <On Platform="Android" Value="white"></On>
                    </OnPlatform>
                </Entry.PlaceholderColor>
                        </Entry>
                </StackLayout>

                <Button Text="Login" Command="{Binding LogInCommand}" FontAttributes="Bold" BackgroundColor="White" BorderRadius="15" TextColor="#166cff" HorizontalOptions="Center" WidthRequest="150" HeightRequest="35" />
                <Label Text="Register?" HorizontalOptions="CenterAndExpand" TextColor="Azure" FontAttributes="Bold" TextDecorations="Underline"  >
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
                        
                    </Label.GestureRecognizers>
                </Label>

            </StackLayout>
        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>
